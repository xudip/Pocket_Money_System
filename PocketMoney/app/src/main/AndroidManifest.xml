<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xudip.pocketmoney">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddIncome" />
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddExpenses" />
        <activity android:name=".Transactions" />
        <activity android:name=".HomePage" />
        <activity android:name=".ViewATransaction" />
        <activity android:name=".Notification" />
        <activity android:name=".Settings" />
        <activity android:name=".ListOfColors"></activity>
    </application>

</manifest>